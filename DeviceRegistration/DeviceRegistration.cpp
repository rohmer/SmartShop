#include "DeviceRegistration.h"

/*
	To test the library, include "DeviceRegistration.h" from an application project
	and call DeviceRegistrationTest().
	
	Do not forget to add the library to Project Dependencies in Visual Studio.
*/

static int s_Test = 0;

int DeviceRegistrationTest()
{
	return ++s_Test;
}